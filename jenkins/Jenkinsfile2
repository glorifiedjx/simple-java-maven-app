node{
  stage('SCM Checkout') {
    git 'https://github.com/glorifiedjx/simple-java-maven-app.git'
  }
  stage('Compile-Package') {
    def mvnHome = tool name: 'maven_3_6_0', type: 'maven'
    echo ${mvnHome}
    sh "${mvnHome}/bin/mvn package"
  }
}
